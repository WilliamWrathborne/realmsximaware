import React, { useEffect, useState } from 'react';
import * as d3 from 'd3';
const D3Visualizer = (props) => {
   const [data, setData] = useState(null);
   useEffect(() => {
      // fetch or axios request to get data from database
      // setData(response)
   }, []);
   return (
   // JSX goes here
   );
};
export default D3Visualizer;
